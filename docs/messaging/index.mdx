import ThemedImage from '@theme/ThemedImage';
import flowchartLightUrl from './flowchart_light_2.png';
import flowchartDarkUrl from './flowchart_dark_2.png';

# System Messaging

Messages are sent between the client and server in the system bi-directionally. This section goes into more depth about the kinds of messages that the client and the server send.

<ThemedImage
	alt="System flowchart"
	sources={{
		light: flowchartLightUrl,
		dark: flowchartDarkUrl,
		}}
/>

